<%= form_for(current_user.users_trips.find_by(trip_id: @trip.id), html: { method: :delete }) do |f| %>
  	<%= f.submit "Leave this Trip", class: "btn", data: { disable_with: "Please wait..." } %>
<% end %>