<% if logged_in? %>
    <div class="well logcursor">
        <h4 class="text-center">Hi, <%= @user.first_name %> <%= @user.last_name %>!</h4>
        <p class="text-center">Welcome back.</p>
        <p class="text-center">
            <%= link_to "Profile", @user, class: 'small-padding btn btn-md woodbtn' %>
            <%= link_to "Logout", logout_path, method: "delete", class: 'small-padding btn btn-md woodbtn' %> 
        </p>
    </div>
<% else %>
    <div class="well logcursor">
        <%= form_for(:session, url: login_path) do |f| %>
            <%= f.email_field :email, :placeholder => "Type your email address here", class: 'form-control wide woodtext' %>
            
            <%= f.password_field :password, :placeholder => "Type your password here", class: 'form-control wide woodtext' %>

            <%= f.label :remember_me, class: "checkbox inline wide" do %>
                <%= f.check_box :remember_me %>
                <span>Keep me logged in</span>
            <% end %>

            <%= f.submit "Log in", class: "btn wide woodbtn logcursor" %>
            
        <% end %>

        <p>New user? <%= link_to "Sign up now!", signup_path %></p>
        Forgot password? <%= link_to "remember", new_password_reset_path %>
    </div>
<% end %>